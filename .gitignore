# Data IS committed now (so `git pull` brings it to the Spark).
# Still never commit generated outputs or model weights.
artifacts/*
hf-cache/*
!**/.gitkeep

# secrets — never commit the real token
.env
__pycache__/
*.pyc
